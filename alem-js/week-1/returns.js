/*
Создайте следующие функции:
id, которая принимает один аргумент и возвращает его. 
getLength, которая принимает массив или строку и возвращает их длину.
*/

const id = (arg) => arg;

const getLength = (array) => array.length;

console.log(id(1));
console.log(getLength([0, 1, 2, 3, 4, 5]));
